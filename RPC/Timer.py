import time

def Contador(limite):
    inicio = time.time()
    fin = 0
    while(fin <= limite):
        fin = time.time()
        fin = fin - inicio

def Interrumpir(limite):
    inicio = time.time()
    fin = 0
    while(fin <= limite):
        fin = time.time()
        fin = fin - inicio
